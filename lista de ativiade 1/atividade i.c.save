#include <stdio.h>

int main(){
    float a, b, c, d;

    printf("Informe a Nota1:");
        scanf("%f", &a);
    printf("Informe a Nota2:");
        scanf("%f", &b);
    printf("Informe a Nota3:");
        scanf("%f", &c);

    d = ((a*2)+(b*3)+(c*5))/10;

    printf("a media final deste aluno e: %.2f", d);
    return 0;
}
